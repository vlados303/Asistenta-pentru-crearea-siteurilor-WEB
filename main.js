document.addEventListener('DOMContentLoaded', () => {
  const an = document.getElementById('an');
  if (an) an.textContent = new Date().getFullYear();
  console.log('Ecomarket: laborator HTML only încărcat.');
});